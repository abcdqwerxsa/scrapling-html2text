微信公众号文章爬取输出示例
================================

输入URL: https://mp.weixin.qq.com/s/EwVItQH4JUsONqv_Fmi4wQ

输出格式:
---

# OpenClaw 永久免费的提取任何网页的终极方案

**作者**: 石臻说AI
**发布时间**:
**原文链接**: https://mp.weixin.qq.com/s/EwVItQH4JUsONqv_Fmi4wQ

---

## 问题是怎么暴露的

在用 AI 做内容创作时，流程大概是这样的：找到一篇参考文章 → 读取全文 → AI 消化后写稿。

`web_fetch` 是最直接的工具，给一个 URL 就返回内容。但用着用着发现不对：

  * 一篇普通技术博客，轻松返回 8000-15000 token
  * 遇到 GitHub README 或文档页，可能更多
  * 一篇文章采集 3-5 个信息源，光读内容就烧掉几万 token

## 三个方案的实测对比

### 方案一：Jina Reader

用法：
```
web_fetch("https://r.jina.ai/原始URL", maxChars=30000)
```

Jina 是专门做网页内容提取的服务，会自动渲染页面、抽取正文、去掉导航和广告，返回干净的 Markdown。

**缺点：每天免费限额 200 次。**

### 方案二：web_fetch 直接抓

测试同一篇文章——直接报错：`fetch failed`。

**结论：只适合静态页面（GitHub README、普通技术博客），不适合有反爬的主流平台。**

### 方案三：Scrapling + html2text

Scrapling 是一个开源 Python 爬虫框架（GitHub: D4Vinci/Scrapling）。

![](https://mmbiz.qpic.cn/sz_mmbiz_jpg/xxx.jpeg)
![Scrapling GitHub 项目主页（24.9k Star）](https://mmbiz.qpic.cn/mmbiz_png/xxx.png)

核心特性：

  * **原生绕过反爬**：StealthyFetcher 能绕过 Cloudflare Turnstile
  * **自适应选择器**：网站改版时能自动重新定位元素
  * **零依赖启动**：`pip install scrapling`

## 最终推荐策略

| 优先级 | 方案 | 适用场景 | 限制 |
|-----|------|---------|------|
| 1 | Jina Reader | 大部分英文博客 | 200次/天 |
| 2 | Scrapling | 微信公众号、反爬平台 | 无限制 |
| 3 | web_fetch | 静态页面 | 全页噪音多 |

![三种方案提取效果对比](https://mmbiz.qpic.cn/sz_mmbiz_png/xxx.png)

## 意外发现：微信公众号文章

测试微信公众号链接时：

  * Jina → 直接 403 拦截，内容为空
  * web_fetch → 请求被中断
  * **Scrapling → 完整拿到正文，图片链接也保留**

---

输出特点:
--------
1. 标题层级完整保留（## ###）
2. 链接格式正确 [文本](URL)
3. 图片格式正确 ![alt](URL)
4. 代码块使用 ``` 格式
5. 表格格式完整
6. 列表格式正确
7. 强调格式保留（**粗体**）
